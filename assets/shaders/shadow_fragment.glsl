#version 330 core

// No output needed - depth is written automatically to depth buffer
// Fragment shader required by OpenGL but can be empty for depth-only pass

void main()
{
    // Depth is automatically written to gl_FragDepth
    // We don't need to output any color
}
